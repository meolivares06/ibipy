import { LastDirective } from './last.directive';

describe('LastDirective', () => {
  it('should create an instance', () => {
    const directive = new LastDirective();
    expect(directive).toBeTruthy();
  });
});
